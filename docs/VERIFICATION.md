# Verification — 7 models × 11 scenarios

Generated by `scripts/verify/report.mjs` from a run's own record (`slots.jsonl`). Do not edit by hand; regenerating overwrites it.

## What was verified

Every slot runs the real path end to end: the real Claude Code binary -> the bridge -> the Copilot SDK -> a Copilot model. Nothing is mocked. Each slot is judged on **files on disk, git history, hook logs and stream-json events**, never on what the model said about its own work.

- Claude Code: `2.1.278`
- Ran: 2026-09-22T00:52:51.013Z → 2026-09-22T01:05:52.772Z
- Took: 782s
- Host: darwin arm64 / node v22.16.0

## Policy, completeness and provenance

- policy: strict-all-pass-v1
- scope: full (77 full-matrix catalogue slots)
- expected: 77 / actual: 77
- result: PASS
- Strict success requires a nonempty, exact, unique expected matrix, all pass outcomes, unchanged user settings and matching start/end code provenance.
- provenance start: commit: c5993091af3aa905d51257130543f82f497fd2f7; dirty: true; fingerprint: sha256 verification-code-v1 ca257bf4ee47104ef0999ceb6d1387e2801164132c054d75b8fcfaf0eafee84c; files: 41
- provenance end: commit: c5993091af3aa905d51257130543f82f497fd2f7; dirty: true; fingerprint: sha256 verification-code-v1 ca257bf4ee47104ef0999ceb6d1387e2801164132c054d75b8fcfaf0eafee84c; files: 41

## Recorded execution settings

| Setting / wait | Recorded value | Applies to |
| --- | --- | --- |
| `--timeout-scale` | 2 | Verification-only timeout multiplier |
| `PENDING_TOOL_WAIT_MS` | 30000 ms (30 s) | Independent pending-tool wait; not multiplied by --timeout-scale |
| `--model-concurrency` | 3 | Concurrent model workers |
| `--scenario-concurrency` | 2 | Concurrent scenario workers per model |
| `bridgeHealthMs` | 240000 ms (240 s) | Verification bridge health wait |
| `planTurnMs` | 180000 ms (180 s) | Each plan-mode headless turn |
| `backgroundLaunchMs` | 240000 ms (240 s) | v11 background launcher invocation |
| `foregroundLaunchMs` | 360000 ms (360 s) | v11 foreground launcher invocation |
| `persistentLaunchMs` | 240000 ms (240 s) | v11 persistent launcher invocation |
| `detachedOutputMs` | 480000 ms (480 s) | v11 detached output wait |
| `scenarioMs.v01-repo-recon` | 480000 ms (480 s) | Per headless turn, not per slot (v01–v10) |
| `scenarioMs.v02-surgical-edit` | 480000 ms (480 s) | Per headless turn, not per slot (v01–v10) |
| `scenarioMs.v03-test-fix-loop` | 480000 ms (480 s) | Per headless turn, not per slot (v01–v10) |
| `scenarioMs.v04-shell-ops` | 840000 ms (840 s) | Per headless turn, not per slot (v01–v10) |
| `scenarioMs.v05-multi-step` | 600000 ms (600 s) | Per headless turn, not per slot (v01–v10) |
| `scenarioMs.v06-subagent` | 480000 ms (480 s) | Per headless turn, not per slot (v01–v10) |
| `scenarioMs.v07-mcp-playwright` | 600000 ms (600 s) | Per headless turn, not per slot (v01–v10) |
| `scenarioMs.v08-hooks-memory` | 660000 ms (660 s) | Per headless turn, not per slot (v01–v10) |
| `scenarioMs.v09-session-resume` | 600000 ms (600 s) | Per headless turn, not per slot (v01–v10) |
| `scenarioMs.v10-long-context` | 480000 ms (480 s) | Per headless turn, not per slot (v01–v10) |
| `scenarioMs.v11-daemon-background` | 840000 ms (840 s) | Planning only; v11 uses the independent step waits above |

Status/list/stop/final-cleanup wrappers, polls/probes, the local-test limit, SIGKILL grace, and operational launcher/daemon startup defaults remain unchanged by the timeout scale.

A command-scoped PENDING_TOOL_WAIT_MS=30000 (30 s) is a verification precaution, not an established fix for no-result exits.

Single-turn schedule estimates are for planning, not deadlines or true worst-case bounds: a slot can contain multiple invocations, and v11 uses independent step waits rather than its scenarioMs planning budget.

## Results

| Scenario | claude-opus-5 | claude-sonnet-5 | claude-haiku-4.5 | gpt-5.6-sol | gpt-5.6-terra | gpt-5.6-luna | gpt-6-astra |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `v01-repo-recon` | PASS | PASS | PASS | PASS | PASS | PASS | PASS |
| `v02-surgical-edit` | PASS | PASS | PASS | PASS | PASS | PASS | PASS |
| `v03-test-fix-loop` | PASS | PASS | PASS | PASS | PASS | PASS | PASS |
| `v04-shell-ops` | PASS | PASS | PASS | PASS | PASS | PASS | PASS |
| `v05-multi-step` | PASS | PASS | PASS | PASS | PASS | PASS | PASS |
| `v06-subagent` | PASS | PASS | PASS | PASS | PASS | PASS | PASS |
| `v07-mcp-playwright` | PASS | PASS | PASS | PASS | PASS | PASS | PASS |
| `v08-hooks-memory` | PASS | PASS | PASS | PASS | PASS | PASS | PASS |
| `v09-session-resume` | PASS | PASS | PASS | PASS | PASS | PASS | PASS |
| `v10-long-context` | PASS | PASS | PASS | PASS | PASS | PASS | PASS |
| `v11-daemon-background` | PASS | PASS | PASS | PASS | PASS | PASS | PASS |

**pass 77 / fail 0 / blocked 0** — 77 slots, gate 77.

BLOCK (blocked) is not a pass. The slot could not be exercised, so it stays in the denominator. DUP marks duplicate records; UNKNOWN marks an unrecognized outcome.

## Scenarios

### `v01-repo-recon` — Repository reconnaissance

Locate code across a repository before touching it, which is the first move of nearly every session.

**Bridge defect this scenario is built to catch**: Large tool_result payloads truncated or re-encoded in transit; multi-call turns reordered so the model cites a path it never read.

Judged on:
- The prompt went in as a stream-json envelope and the CLI replayed it back.
- A search tool was actually used (Bash — this build offers no Glob or Grep).
- The answer names the planted file, not either decoy.
- Every tool_use in the transcript has a matching tool_result.
- result carries a stop_reason and non-zero usage, and modelUsage names the expected backend.

### `v02-surgical-edit` — Surgical edit and file creation

Change exactly what was asked and nothing else, then create a new file. The edit contract is byte-level.

**Bridge defect this scenario is built to catch**: Whitespace or newline normalisation in the tool_use payload makes Edit's exact-match fail, or silently matches the wrong line.

Judged on:
- A plan-mode turn finished on its own and left the file untouched.
- The second marked line holds the new value.
- The first and third marked lines are byte-for-byte unchanged.
- The requested new file exists with the requested export.
- The Edit tool was used; the file was not rewritten wholesale.

### `v03-test-fix-loop` — Failing test diagnose and fix

The core agentic loop: run, read the failure, change the source, run again until green.

**Bridge defect this scenario is built to catch**: A non-zero exit or stderr-heavy tool_result is dropped or mis-flagged, so the model never learns the run failed and stops early.

Judged on:
- Bash was used to run the suite.
- The first run came back failing and the session continued anyway.
- The test file is byte-for-byte unchanged.
- The runner re-runs the suite itself afterwards and it exits 0.

### `v04-shell-ops` — Background shell and git workflow

Long-running processes and version control, the two shell workflows that outlive a single command.

**Bridge defect this scenario is built to catch**: A backgrounded shell's handle is lost across turns, so the model never sees it finish and the process is orphaned.

Judged on:
- A long-running process was started and watched to completion.
- The tick log reached at least three lines.
- git log shows exactly the requested commit subject.
- Exactly one secondary worktree matches the requested branch or its native EnterWorktree equivalent.
- The same worktree holds the marker file both on disk and committed unchanged in HEAD.
- The marker file is absent from both the primary checkout and its HEAD.
- No ticker process survives the slot.

### `v05-multi-step` — Four-step plan across file types

Holding a four-step plan over one turn and landing every step, across a source file, a plain text file, a new markdown file and a Jupyter notebook.

**Bridge defect this scenario is built to catch**: The plan is lost as the tool loop lengthens, so early steps land and the last ones are silently dropped.

Judged on:
- All four changes are present on disk.
- The notebook still parses as a valid nbformat 4 document.
- No step was reported done without the file backing it.
- The tokens inside the attached PDF and PNG both came back.

### `v06-subagent` — Subagent delegation

Handing work to a project-defined subagent and using what comes back.

**Bridge defect this scenario is built to catch**: The subagent's own tool loop runs on a nested session; its final report can be lost at the handoff, leaving the parent to invent an answer.

Judged on:
- The project-defined agent is advertised in the init event.
- The Task tool was invoked against that agent.
- A tool_result came back for the delegation.
- The final answer names the files only the delegated search could have found.

### `v07-mcp-playwright` — MCP browser automation, headless

Third-party capability arriving over MCP and being used for real: a headless Chrome driven through @playwright/mcp.

**Bridge defect this scenario is built to catch**: MCP tool schemas are rewritten or namespaced differently on the way through, so the model cannot call them or calls them with the wrong shape.

Judged on:
- The playwright MCP server is connected in the init event.
- At least one mcp__playwright__* tool was called.
- The answer carries the token that only exists in the served page's DOM.

### `v08-hooks-memory` — Hooks, memory, commands and skills

Everything the project configures around the model: CLAUDE.md rules, hooks that observe and deny, custom commands and skills.

**Bridge defect this scenario is built to catch**: A hook denial arrives as an ordinary tool_result instead of a refusal, so the model treats a blocked action as done.

Judged on:
- The CLAUDE.md rule was obeyed: the record landed at the path the project mandates.
- The PreToolUse hook fired and left its log.
- The forbidden command was denied and never ran.
- The custom command and skill are advertised in the init event.
- A plugin loaded from --plugin-dir contributes a command and a skill, both advertised.
- A cron job was created and came back out of the job list, carrying the id or the prompt it was created with.

### `v09-session-resume` — Session resume across processes

Closing the laptop and picking the same conversation back up: later processes must recall literal values from conversation context, without tools or persistent memory.

**Bridge defect this scenario is built to catch**: Session state is keyed on the upstream connection, so resuming re-opens an empty conversation that answers from nothing.

Judged on:
- The resumed process reports the same session id.
- It recalls the planted build id.
- The seed, resumed, and forked processes use no tools, including persistent-memory reads or writes.
- A forked resume inherits the same context under a session id of its own.
- The fork recalls the original deploy-window string without adding a calendar date.

### `v10-long-context` — Long context retrieval and reasoning

Pulling two facts from far apart in a large context and combining them, which is what long context is actually for.

**Bridge defect this scenario is built to catch**: Context is silently truncated upstream, so the model answers confidently from the half it still has. The corpus is carried in the prompt, not read from disk, so nothing but the transport can deliver it.

Judged on:
- The result carries a schema-valid structured object, not just prose.
- The arithmetic answer is exactly right, which needs both facts.
- Reported input usage reflects the whole corpus, not a truncated prefix.
- The answer came from the prompt itself: no file-reading tool was used.

### `v11-daemon-background` — Launcher, daemon and background agent

The front door this project actually ships: the bin/claude-ghcp launcher, the bridge daemon it leaves running, and a detached agent that keeps being served after the launcher exits.

**Bridge defect this scenario is built to catch**: The daemon outlives the process that started it, so its lifetime is owned by a registry file rather than a parent. A stale registry hands out a dead port; a second launch quietly starts a rival daemon instead of reusing the live one; stop leaves the port bound. None of that is visible to a scenario that spawns its own short-lived bridge.

Judged on:
- The launcher's own preflight passed and it reported a backgrounded session id.
- claude-ghcp-status reports the daemon running, with a pid and port, under the requested model.
- The detached agent wrote a value that exists only in a file it had to read.
- claude agents lists the background session against the slot's workspace.
- A foreground launch answers from its own ephemeral bridge and leaves the daemon undisturbed.
- A launch that asks for the persistent bridge goes through the daemon and reuses the same pid and port instead of starting a rival.
- claude-ghcp-stop reports stopped, status goes not-running, and the registry and log are gone.

## Coverage

The 11 selected scenarios map to **92.2%** of the current Claude Code core-feature inventory (weight 94/102). This is catalogue coverage, not a pass rate or proof of complete execution. It is computed from `scripts/verify/features.mjs` and each selected scenario's `covers` list, independently of the result tally.

### Not covered (the honest remainder)

- `thinking` (weight 2) — Extended thinking / reasoning blocks
- `webfetch` (weight 2) — WebFetch / WebSearch
- `tui` (weight 2) — Interactive TUI affordances (plan picker, /rewind)
- `compaction` (weight 2) — Automatic context compaction

### Tools this build does not offer (excluded from the denominator)

Claude Code 2.1.278 does not provide `TodoWrite`, `BashOutput`, `KillShell`, `Glob`, `Grep`. Measured by `scripts/verify/probe.mjs`, which instructs a session to call each one by name and reads the stream: a tool the build does not offer can never produce a tool_use block, however firmly it is asked for. Read runs in the same turn as a positive control, so a model that declines wholesale is distinguishable from a tool that is genuinely missing. That is the CLI's boundary rather than a bridge defect, so they count neither as covered nor as missed and leave the denominator entirely.

- `glob` (weight 3, `Glob` needs) — Glob path matching
- `grep` (weight 3, `Grep` needs) — Grep content search
- `bash-background` (weight 2, `BashOutput` needs) — Background shells and output polling
- `todo` (weight 2, `TodoWrite` needs) — TodoWrite task tracking

## Reproducing

These commands reuse the recorded settings. Any unrecorded settings still use current defaults, not reconstructed historical values.

```bash
PENDING_TOOL_WAIT_MS=30000 npm run verify -- --timeout-scale 2 --model-concurrency 3 --scenario-concurrency 2 # 7 models × 11 scenarios
PENDING_TOOL_WAIT_MS=30000 npm run verify -- --timeout-scale 2 --model-concurrency 3 --scenario-concurrency 2 --dry-run # plan only, no model calls
npm run verify:probe        # capability probes behind the absent-tool list
npm run verify:report       # summarise the most recent run
```

